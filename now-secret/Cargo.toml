[package]
name = "now-secret"
version = "0.1.0"
edition = "2024"
description = "Secret handling for now"
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
zeroize = { workspace = true }
